const taskInput = document.getElementById("taskInput")
console.log(taskInput)

const priority = document.getElementById("priority")
console.log(priority)

const dueDate = document.getElementById("dueDate");
console.log(dueDate);

const addBtn = document.getElementById("addBtn");
console.log(addBtn);

const search = document.getElementById("search")
console.log(search)

const filter = document.getElementById("filter")
console.log(filter)

const taskContainer = document.getElementById("taskContainer")
console.log(taskContainer)

// adding the event listner

let tasks = []
    
addBtn.addEventListener("click",function(){
    const taskName = taskInput.value;
    const p = priority.value;
    const DueDate = dueDate.value;

    const task = {
        Name : taskName,
        Priority : p,
        DueDate: DueDate
    };

    tasks.push(task)
    renderTasks();

    console.log(tasks)


    const taskName1 = document.createElement("p");
    taskName1.textContent = task.Name;
    taskCard.appendChild(taskName1);
    const taskP =document.createElement("p");
    taskP.textContent = task.Priority;
    taskCard.appendChild(taskP);
    const taskDD = document.createElement("p");
    taskDD.textContent = task.DueDate;
    taskCard.appendChild(taskDD);
    taskContainer.appendChild(taskCard);

    const deleteBtn = document.createElement("button");
    deleteBtn.textContent = "Delete";
    taskCard.appendChild(deleteBtn);

    deleteBtn.addEventListener("click", function() {
    taskCard.remove()
    });

    console.log(taskCard)

});

function renderTasks(){
    taskContainer.innerHTML = "";
        tasks.forEach(function(task){
            const taskCard = document.createElement("div");
            taskCard.className = "task-card";

            taskContainer.appendChild(taskCard);
            console.log(task);
        })
}















